{"title":"Linux（Ubuntu）使用无线鼠标有延迟的解决方法","date":"2024-02-18T03:20:55.000Z","source":"_posts/Linux（Ubuntu）使用无线鼠标有延迟.md","raw":"---\ntitle: Linux（Ubuntu）使用无线鼠标有延迟的解决方法\ncategories: [OS]\ntags: [Linux, Ubuntu]\n\ndate: 2024/2/18 11:20:55\n\n---\n\n## 问题描述\n\n在 Linux（尤其是 Ubuntu）系统中使用无线鼠标（如 2.4G 接收器或蓝牙鼠标）时，可能会遇到鼠标移动不跟手、有延迟、掉帧甚至偶尔卡顿的情况。这个问题在树莓派等 ARM 设备上尤为常见，但 x86 平台的 Ubuntu 也可能遇到。\n\n## 原因分析\n\n鼠标延迟通常与以下因素有关：\n\n1. **USB 轮询频率（Polling Rate）**：Linux 内核默认对 USB 设备采用较低的轮询频率，这会限制鼠标报告位置的频率，导致感知上的延迟。\n2. **无线信号干扰**：2.4G Wi-Fi、蓝牙设备或其他无线外设可能产生干扰。\n3. **电源管理**：系统的 USB 自动休眠策略可能会让无线接收器进入低功耗状态，唤醒时产生延迟。\n4. **桌面环境 compositor**：某些桌面环境的合成器设置也可能引入额外的显示延迟。\n\n本文主要解决的是 **USB 轮询频率** 导致的延迟问题。\n\n## 解决方案\n\n### 方法一：修改内核启动参数（推荐）\n\n通过在内核启动参数中添加 `usbhid.mousepoll=0`，可以让系统使用鼠标设备自身的轮询频率（通常为 1000Hz），而不是内核默认的较低频率。\n\n#### 步骤\n\n**1. 找到启动配置文件**\n\n首先查看 `/boot/firmware/config.txt`（树莓派等 ARM 设备）或 `/etc/default/grub`（x86 设备）：\n\n```shell\n# 对于树莓派等使用 config.txt 的设备\ncat /boot/firmware/config.txt | grep cmdline\n# 输出示例：cmdline=cmdline.txt\n```\n\n**2. 编辑启动参数文件**\n\n```shell\nsudo vim /boot/firmware/cmdline.txt\n```\n\n> 注意：文件名可能与 `config.txt` 中配置的 `cmdline=xxx.txt` 一致，常见为 `cmdline.txt`。\n\n在文件末尾添加以下参数（确保与已有参数用空格分隔）：\n\n```\nusbhid.mousepoll=0\n```\n\n完整示例：\n\n```\nconsole=serial0,115200 console=tty1 root=PARTUUID=xxxxxx-xx rootfstype=ext4 fsck.repair=yes rootwait usbhid.mousepoll=0\n```\n\n**3. 重启系统**\n\n```shell\nsudo reboot\n```\n\n**4. 验证是否生效**\n\n重启后，可以通过以下命令检查当前轮询间隔：\n\n```shell\ncat /sys/module/usbhid/parameters/mousepoll\n```\n\n如果输出为 `0`，则表示配置已生效。\n\n### 方法二：使用 modprobe 配置（临时/永久）\n\n#### 临时生效（重启后失效）\n\n```shell\nsudo modprobe -r usbhid\nsudo modprobe usbhid mousepoll=0\n```\n\n#### 永久生效\n\n创建或编辑 modprobe 配置文件：\n\n```shell\necho \"options usbhid mousepoll=0\" | sudo tee /etc/modprobe.d/usbhid.conf\nsudo update-initramfs -u\nsudo reboot\n```\n\n### 方法三：调整 USB 电源管理\n\n如果延迟是由 USB 自动休眠引起的，可以禁用 USB 设备的电源管理：\n\n```shell\n# 查看当前 USB 设备的电源管理状态\nfor f in /sys/bus/usb/devices/*/power/control; do echo \"$f: $(cat $f)\"; done\n\n# 禁用所有 USB 设备的自动休眠（需要 root）\nfor f in /sys/bus/usb/devices/*/power/control; do echo \"on\" | sudo tee $f > /dev/null; done\n```\n\n要让设置永久生效，可以创建一个 udev 规则：\n\n```shell\necho 'ACTION==\"add\", SUBSYSTEM==\"usb\", TEST==\"power/control\", ATTR{power/control}=\"on\"' | sudo tee /etc/udev/rules.d/50-usb-power-save.rules\nsudo udevadm control --reload-rules\n```\n\n## 补充优化\n\n### 使用 `evhz` 测试鼠标轮询率\n\n可以安装 `evhz` 工具来实时查看鼠标的报告频率：\n\n```shell\n# 安装 evhz\ngit clone https://github.com/ian-kent/evhz.git\ncd evhz\nmake\nsudo ./evhz\n```\n\n运行后移动鼠标，可以看到实时的轮询频率。配置生效后，数值应该稳定在 1000Hz 左右（取决于鼠标本身的支持）。\n\n### 蓝牙鼠标的额外优化\n\n如果是蓝牙鼠标，还可以尝试：\n\n```shell\n# 查看蓝牙连接参数\nhciconfig\n\n# 调整蓝牙连接间隔（需要 root，值越小延迟越低，但耗电增加）\nsudo hcitool cmd 0x08 0x0013 <handle> <min_interval> <max_interval> <latency> <timeout>\n```\n\n## 注意事项\n\n- `mousepoll=0` 表示使用设备的默认轮询间隔。如果你的鼠标本身质量较差，可能并不能完全解决问题。\n- 部分老旧无线鼠标的默认轮询率可能只有 125Hz，这种情况下即使设置 `mousepoll=0` 也无法达到 1000Hz。\n- 树莓派等设备的 USB 带宽有限，如果同时连接多个高带宽 USB 设备（如摄像头、外置硬盘），可能仍然会有延迟。\n- 修改内核启动参数前建议备份原配置，以便恢复。\n\n","slug":"Linux（Ubuntu）使用无线鼠标有延迟","published":true,"updated":"2026-05-27T08:39:39.121Z","_id":"cuidEG332o6_n-po135wvpjEB","comments":true,"layout":"post","photos":[],"html":"<h2 id=\"问题描述\"><a href=\"#问题描述\" class=\"headerlink\" title=\"问题描述\"></a>问题描述</h2><p>在 Linux（尤其是 Ubuntu）系统中使用无线鼠标（如 2.4G 接收器或蓝牙鼠标）时，可能会遇到鼠标移动不跟手、有延迟、掉帧甚至偶尔卡顿的情况。这个问题在树莓派等 ARM 设备上尤为常见，但 x86 平台的 Ubuntu 也可能遇到。</p>\n<h2 id=\"原因分析\"><a href=\"#原因分析\" class=\"headerlink\" title=\"原因分析\"></a>原因分析</h2><p>鼠标延迟通常与以下因素有关：</p>\n<ol>\n<li><strong>USB 轮询频率（Polling Rate）</strong>：Linux 内核默认对 USB 设备采用较低的轮询频率，这会限制鼠标报告位置的频率，导致感知上的延迟。</li>\n<li><strong>无线信号干扰</strong>：2.4G Wi-Fi、蓝牙设备或其他无线外设可能产生干扰。</li>\n<li><strong>电源管理</strong>：系统的 USB 自动休眠策略可能会让无线接收器进入低功耗状态，唤醒时产生延迟。</li>\n<li><strong>桌面环境 compositor</strong>：某些桌面环境的合成器设置也可能引入额外的显示延迟。</li>\n</ol>\n<p>本文主要解决的是 <strong>USB 轮询频率</strong> 导致的延迟问题。</p>\n<h2 id=\"解决方案\"><a href=\"#解决方案\" class=\"headerlink\" title=\"解决方案\"></a>解决方案</h2><h3 id=\"方法一：修改内核启动参数（推荐）\"><a href=\"#方法一：修改内核启动参数（推荐）\" class=\"headerlink\" title=\"方法一：修改内核启动参数（推荐）\"></a>方法一：修改内核启动参数（推荐）</h3><p>通过在内核启动参数中添加 <code>usbhid.mousepoll=0</code>，可以让系统使用鼠标设备自身的轮询频率（通常为 1000Hz），而不是内核默认的较低频率。</p>\n<h4 id=\"步骤\"><a href=\"#步骤\" class=\"headerlink\" title=\"步骤\"></a>步骤</h4><p><strong>1. 找到启动配置文件</strong></p>\n<p>首先查看 <code>/boot/firmware/config.txt</code>（树莓派等 ARM 设备）或 <code>/etc/default/grub</code>（x86 设备）：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">对于树莓派等使用 config.txt 的设备</span></span><br><span class=\"line\">cat /boot/firmware/config.txt | grep cmdline</span><br><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">输出示例：cmdline=cmdline.txt</span></span><br></pre></td></tr></table></figure>\n\n<p><strong>2. 编辑启动参数文件</strong></p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">sudo vim /boot/firmware/cmdline.txt</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>注意：文件名可能与 <code>config.txt</code> 中配置的 <code>cmdline=xxx.txt</code> 一致，常见为 <code>cmdline.txt</code>。</p>\n</blockquote>\n<p>在文件末尾添加以下参数（确保与已有参数用空格分隔）：</p>\n<figure class=\"highlight plaintext\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">usbhid.mousepoll=0</span><br></pre></td></tr></table></figure>\n\n<p>完整示例：</p>\n<figure class=\"highlight plaintext\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">console=serial0,115200 console=tty1 root=PARTUUID=xxxxxx-xx rootfstype=ext4 fsck.repair=yes rootwait usbhid.mousepoll=0</span><br></pre></td></tr></table></figure>\n\n<p><strong>3. 重启系统</strong></p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">sudo reboot</span><br></pre></td></tr></table></figure>\n\n<p><strong>4. 验证是否生效</strong></p>\n<p>重启后，可以通过以下命令检查当前轮询间隔：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">cat /sys/module/usbhid/parameters/mousepoll</span><br></pre></td></tr></table></figure>\n\n<p>如果输出为 <code>0</code>，则表示配置已生效。</p>\n<h3 id=\"方法二：使用-modprobe-配置（临时-永久）\"><a href=\"#方法二：使用-modprobe-配置（临时-永久）\" class=\"headerlink\" title=\"方法二：使用 modprobe 配置（临时&#x2F;永久）\"></a>方法二：使用 modprobe 配置（临时&#x2F;永久）</h3><h4 id=\"临时生效（重启后失效）\"><a href=\"#临时生效（重启后失效）\" class=\"headerlink\" title=\"临时生效（重启后失效）\"></a>临时生效（重启后失效）</h4><figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">sudo modprobe -r usbhid</span><br><span class=\"line\">sudo modprobe usbhid mousepoll=0</span><br></pre></td></tr></table></figure>\n\n<h4 id=\"永久生效\"><a href=\"#永久生效\" class=\"headerlink\" title=\"永久生效\"></a>永久生效</h4><p>创建或编辑 modprobe 配置文件：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">echo &quot;options usbhid mousepoll=0&quot; | sudo tee /etc/modprobe.d/usbhid.conf</span><br><span class=\"line\">sudo update-initramfs -u</span><br><span class=\"line\">sudo reboot</span><br></pre></td></tr></table></figure>\n\n<h3 id=\"方法三：调整-USB-电源管理\"><a href=\"#方法三：调整-USB-电源管理\" class=\"headerlink\" title=\"方法三：调整 USB 电源管理\"></a>方法三：调整 USB 电源管理</h3><p>如果延迟是由 USB 自动休眠引起的，可以禁用 USB 设备的电源管理：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">查看当前 USB 设备的电源管理状态</span></span><br><span class=\"line\">for f in /sys/bus/usb/devices/*/power/control; do echo &quot;$f: $(cat $f)&quot;; done</span><br><span class=\"line\"><span class=\"meta prompt_\"></span></span><br><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">禁用所有 USB 设备的自动休眠（需要 root）</span></span><br><span class=\"line\">for f in /sys/bus/usb/devices/*/power/control; do echo &quot;on&quot; | sudo tee $f &gt; /dev/null; done</span><br></pre></td></tr></table></figure>\n\n<p>要让设置永久生效，可以创建一个 udev 规则：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">echo &#x27;ACTION==&quot;add&quot;, SUBSYSTEM==&quot;usb&quot;, TEST==&quot;power/control&quot;, ATTR&#123;power/control&#125;=&quot;on&quot;&#x27; | sudo tee /etc/udev/rules.d/50-usb-power-save.rules</span><br><span class=\"line\">sudo udevadm control --reload-rules</span><br></pre></td></tr></table></figure>\n\n<h2 id=\"补充优化\"><a href=\"#补充优化\" class=\"headerlink\" title=\"补充优化\"></a>补充优化</h2><h3 id=\"使用-evhz-测试鼠标轮询率\"><a href=\"#使用-evhz-测试鼠标轮询率\" class=\"headerlink\" title=\"使用 evhz 测试鼠标轮询率\"></a>使用 <code>evhz</code> 测试鼠标轮询率</h3><p>可以安装 <code>evhz</code> 工具来实时查看鼠标的报告频率：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">安装 evhz</span></span><br><span class=\"line\">git clone https://github.com/ian-kent/evhz.git</span><br><span class=\"line\">cd evhz</span><br><span class=\"line\">make</span><br><span class=\"line\">sudo ./evhz</span><br></pre></td></tr></table></figure>\n\n<p>运行后移动鼠标，可以看到实时的轮询频率。配置生效后，数值应该稳定在 1000Hz 左右（取决于鼠标本身的支持）。</p>\n<h3 id=\"蓝牙鼠标的额外优化\"><a href=\"#蓝牙鼠标的额外优化\" class=\"headerlink\" title=\"蓝牙鼠标的额外优化\"></a>蓝牙鼠标的额外优化</h3><p>如果是蓝牙鼠标，还可以尝试：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">查看蓝牙连接参数</span></span><br><span class=\"line\">hciconfig</span><br><span class=\"line\"><span class=\"meta prompt_\"></span></span><br><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">调整蓝牙连接间隔（需要 root，值越小延迟越低，但耗电增加）</span></span><br><span class=\"line\">sudo hcitool cmd 0x08 0x0013 &lt;handle&gt; &lt;min_interval&gt; &lt;max_interval&gt; &lt;latency&gt; &lt;timeout&gt;</span><br></pre></td></tr></table></figure>\n\n<h2 id=\"注意事项\"><a href=\"#注意事项\" class=\"headerlink\" title=\"注意事项\"></a>注意事项</h2><ul>\n<li><code>mousepoll=0</code> 表示使用设备的默认轮询间隔。如果你的鼠标本身质量较差，可能并不能完全解决问题。</li>\n<li>部分老旧无线鼠标的默认轮询率可能只有 125Hz，这种情况下即使设置 <code>mousepoll=0</code> 也无法达到 1000Hz。</li>\n<li>树莓派等设备的 USB 带宽有限，如果同时连接多个高带宽 USB 设备（如摄像头、外置硬盘），可能仍然会有延迟。</li>\n<li>修改内核启动参数前建议备份原配置，以便恢复。</li>\n</ul>\n","excerpt":"","more":"<h2 id=\"问题描述\"><a href=\"#问题描述\" class=\"headerlink\" title=\"问题描述\"></a>问题描述</h2><p>在 Linux（尤其是 Ubuntu）系统中使用无线鼠标（如 2.4G 接收器或蓝牙鼠标）时，可能会遇到鼠标移动不跟手、有延迟、掉帧甚至偶尔卡顿的情况。这个问题在树莓派等 ARM 设备上尤为常见，但 x86 平台的 Ubuntu 也可能遇到。</p>\n<h2 id=\"原因分析\"><a href=\"#原因分析\" class=\"headerlink\" title=\"原因分析\"></a>原因分析</h2><p>鼠标延迟通常与以下因素有关：</p>\n<ol>\n<li><strong>USB 轮询频率（Polling Rate）</strong>：Linux 内核默认对 USB 设备采用较低的轮询频率，这会限制鼠标报告位置的频率，导致感知上的延迟。</li>\n<li><strong>无线信号干扰</strong>：2.4G Wi-Fi、蓝牙设备或其他无线外设可能产生干扰。</li>\n<li><strong>电源管理</strong>：系统的 USB 自动休眠策略可能会让无线接收器进入低功耗状态，唤醒时产生延迟。</li>\n<li><strong>桌面环境 compositor</strong>：某些桌面环境的合成器设置也可能引入额外的显示延迟。</li>\n</ol>\n<p>本文主要解决的是 <strong>USB 轮询频率</strong> 导致的延迟问题。</p>\n<h2 id=\"解决方案\"><a href=\"#解决方案\" class=\"headerlink\" title=\"解决方案\"></a>解决方案</h2><h3 id=\"方法一：修改内核启动参数（推荐）\"><a href=\"#方法一：修改内核启动参数（推荐）\" class=\"headerlink\" title=\"方法一：修改内核启动参数（推荐）\"></a>方法一：修改内核启动参数（推荐）</h3><p>通过在内核启动参数中添加 <code>usbhid.mousepoll=0</code>，可以让系统使用鼠标设备自身的轮询频率（通常为 1000Hz），而不是内核默认的较低频率。</p>\n<h4 id=\"步骤\"><a href=\"#步骤\" class=\"headerlink\" title=\"步骤\"></a>步骤</h4><p><strong>1. 找到启动配置文件</strong></p>\n<p>首先查看 <code>/boot/firmware/config.txt</code>（树莓派等 ARM 设备）或 <code>/etc/default/grub</code>（x86 设备）：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">对于树莓派等使用 config.txt 的设备</span></span><br><span class=\"line\">cat /boot/firmware/config.txt | grep cmdline</span><br><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">输出示例：cmdline=cmdline.txt</span></span><br></pre></td></tr></table></figure>\n\n<p><strong>2. 编辑启动参数文件</strong></p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">sudo vim /boot/firmware/cmdline.txt</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>注意：文件名可能与 <code>config.txt</code> 中配置的 <code>cmdline=xxx.txt</code> 一致，常见为 <code>cmdline.txt</code>。</p>\n</blockquote>\n<p>在文件末尾添加以下参数（确保与已有参数用空格分隔）：</p>\n<figure class=\"highlight plaintext\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">usbhid.mousepoll=0</span><br></pre></td></tr></table></figure>\n\n<p>完整示例：</p>\n<figure class=\"highlight plaintext\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">console=serial0,115200 console=tty1 root=PARTUUID=xxxxxx-xx rootfstype=ext4 fsck.repair=yes rootwait usbhid.mousepoll=0</span><br></pre></td></tr></table></figure>\n\n<p><strong>3. 重启系统</strong></p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">sudo reboot</span><br></pre></td></tr></table></figure>\n\n<p><strong>4. 验证是否生效</strong></p>\n<p>重启后，可以通过以下命令检查当前轮询间隔：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">cat /sys/module/usbhid/parameters/mousepoll</span><br></pre></td></tr></table></figure>\n\n<p>如果输出为 <code>0</code>，则表示配置已生效。</p>\n<h3 id=\"方法二：使用-modprobe-配置（临时-永久）\"><a href=\"#方法二：使用-modprobe-配置（临时-永久）\" class=\"headerlink\" title=\"方法二：使用 modprobe 配置（临时&#x2F;永久）\"></a>方法二：使用 modprobe 配置（临时&#x2F;永久）</h3><h4 id=\"临时生效（重启后失效）\"><a href=\"#临时生效（重启后失效）\" class=\"headerlink\" title=\"临时生效（重启后失效）\"></a>临时生效（重启后失效）</h4><figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">sudo modprobe -r usbhid</span><br><span class=\"line\">sudo modprobe usbhid mousepoll=0</span><br></pre></td></tr></table></figure>\n\n<h4 id=\"永久生效\"><a href=\"#永久生效\" class=\"headerlink\" title=\"永久生效\"></a>永久生效</h4><p>创建或编辑 modprobe 配置文件：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">echo &quot;options usbhid mousepoll=0&quot; | sudo tee /etc/modprobe.d/usbhid.conf</span><br><span class=\"line\">sudo update-initramfs -u</span><br><span class=\"line\">sudo reboot</span><br></pre></td></tr></table></figure>\n\n<h3 id=\"方法三：调整-USB-电源管理\"><a href=\"#方法三：调整-USB-电源管理\" class=\"headerlink\" title=\"方法三：调整 USB 电源管理\"></a>方法三：调整 USB 电源管理</h3><p>如果延迟是由 USB 自动休眠引起的，可以禁用 USB 设备的电源管理：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">查看当前 USB 设备的电源管理状态</span></span><br><span class=\"line\">for f in /sys/bus/usb/devices/*/power/control; do echo &quot;$f: $(cat $f)&quot;; done</span><br><span class=\"line\"><span class=\"meta prompt_\"></span></span><br><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">禁用所有 USB 设备的自动休眠（需要 root）</span></span><br><span class=\"line\">for f in /sys/bus/usb/devices/*/power/control; do echo &quot;on&quot; | sudo tee $f &gt; /dev/null; done</span><br></pre></td></tr></table></figure>\n\n<p>要让设置永久生效，可以创建一个 udev 规则：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">echo &#x27;ACTION==&quot;add&quot;, SUBSYSTEM==&quot;usb&quot;, TEST==&quot;power/control&quot;, ATTR&#123;power/control&#125;=&quot;on&quot;&#x27; | sudo tee /etc/udev/rules.d/50-usb-power-save.rules</span><br><span class=\"line\">sudo udevadm control --reload-rules</span><br></pre></td></tr></table></figure>\n\n<h2 id=\"补充优化\"><a href=\"#补充优化\" class=\"headerlink\" title=\"补充优化\"></a>补充优化</h2><h3 id=\"使用-evhz-测试鼠标轮询率\"><a href=\"#使用-evhz-测试鼠标轮询率\" class=\"headerlink\" title=\"使用 evhz 测试鼠标轮询率\"></a>使用 <code>evhz</code> 测试鼠标轮询率</h3><p>可以安装 <code>evhz</code> 工具来实时查看鼠标的报告频率：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">安装 evhz</span></span><br><span class=\"line\">git clone https://github.com/ian-kent/evhz.git</span><br><span class=\"line\">cd evhz</span><br><span class=\"line\">make</span><br><span class=\"line\">sudo ./evhz</span><br></pre></td></tr></table></figure>\n\n<p>运行后移动鼠标，可以看到实时的轮询频率。配置生效后，数值应该稳定在 1000Hz 左右（取决于鼠标本身的支持）。</p>\n<h3 id=\"蓝牙鼠标的额外优化\"><a href=\"#蓝牙鼠标的额外优化\" class=\"headerlink\" title=\"蓝牙鼠标的额外优化\"></a>蓝牙鼠标的额外优化</h3><p>如果是蓝牙鼠标，还可以尝试：</p>\n<figure class=\"highlight shell\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">查看蓝牙连接参数</span></span><br><span class=\"line\">hciconfig</span><br><span class=\"line\"><span class=\"meta prompt_\"></span></span><br><span class=\"line\"><span class=\"meta prompt_\"># </span><span class=\"language-bash\">调整蓝牙连接间隔（需要 root，值越小延迟越低，但耗电增加）</span></span><br><span class=\"line\">sudo hcitool cmd 0x08 0x0013 &lt;handle&gt; &lt;min_interval&gt; &lt;max_interval&gt; &lt;latency&gt; &lt;timeout&gt;</span><br></pre></td></tr></table></figure>\n\n<h2 id=\"注意事项\"><a href=\"#注意事项\" class=\"headerlink\" title=\"注意事项\"></a>注意事项</h2><ul>\n<li><code>mousepoll=0</code> 表示使用设备的默认轮询间隔。如果你的鼠标本身质量较差，可能并不能完全解决问题。</li>\n<li>部分老旧无线鼠标的默认轮询率可能只有 125Hz，这种情况下即使设置 <code>mousepoll=0</code> 也无法达到 1000Hz。</li>\n<li>树莓派等设备的 USB 带宽有限，如果同时连接多个高带宽 USB 设备（如摄像头、外置硬盘），可能仍然会有延迟。</li>\n<li>修改内核启动参数前建议备份原配置，以便恢复。</li>\n</ul>\n","path":"2024/02/18/Linux（Ubuntu）使用无线鼠标有延迟/","permalink":"https://935577.xyz/2024/02/18/Linux%EF%BC%88Ubuntu%EF%BC%89%E4%BD%BF%E7%94%A8%E6%97%A0%E7%BA%BF%E9%BC%A0%E6%A0%87%E6%9C%89%E5%BB%B6%E8%BF%9F/","tags":[{"name":"Linux","_id":"cuidg3eAsvrnhX3tvPEw2KkFR","slug":"Linux","path":"tags/Linux/","permalink":"https://935577.xyz/tags/Linux/","length":2},{"name":"Ubuntu","_id":"cuidHDYeXqVgW87W59OJabirr","slug":"Ubuntu","path":"tags/Ubuntu/","permalink":"https://935577.xyz/tags/Ubuntu/","length":1}],"categories":[{"name":"OS","_id":"cuidflDT0Aws3O-Kqj115xexR","slug":"OS","path":"categories/OS/","permalink":"https://935577.xyz/categories/OS/","length":4}],"prev":{"title":"macOS 移除 ABC 输入法（仅保留第三方输入法）","date":"2025-01-07T23:50:05.000Z","slug":"关闭Mac OS ABC输入法","published":true,"updated":"2026-05-27T08:39:39.121Z","_id":"cuidWIFZAOtx_slYuKp-vss5P","layout":"post","photos":[],"excerpt":"","path":"2025/01/07/关闭Mac OS ABC输入法/","permalink":"https://935577.xyz/2025/01/07/%E5%85%B3%E9%97%ADMac%20OS%20ABC%E8%BE%93%E5%85%A5%E6%B3%95/","__post":true},"next":{"title":"Java Stream 流详解","date":"2023-11-05T08:45:33.000Z","slug":"Java-Stream流","published":true,"updated":"2026-05-27T08:39:39.121Z","_id":"cuid51FduXAGqlIPpzoyTiI7J","layout":"post","photos":[],"excerpt":"","path":"2023/11/05/Java-Stream流/","permalink":"https://935577.xyz/2023/11/05/Java-Stream%E6%B5%81/","__post":true},"__post":true}